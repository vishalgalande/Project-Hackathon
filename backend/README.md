# Backend Directory

Place all Python backend code here.
- Create a virtual environment: `python -m venv venv`
- Activate it: `.\venv\Scripts\activate`
- Install deps: `pip install -r requirements.txt`
